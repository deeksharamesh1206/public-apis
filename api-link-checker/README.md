# API Link Checker

This tool checks whether API links are active or broken.

## Description
The API Link Checker sends HTTP requests to each API URL and verifies its availability using response status codes.

## How to Run
1. Open terminal in the project folder
2. Run:
   python api_link_checker.py

## Output
- Displays working API links
- Flags broken or unreachable links
